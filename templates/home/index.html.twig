{% extends 'base.html.twig' %}

{% block title %}Stubborn{% endblock %}

{% block body %}


<div class="container"><br><br>
    <h1>Bienvenue chez Stubborn</h1><br>
    <div class="row">

    {% for product in products %}
        <div class="col-md-3 mt-4">

            <div class="card" style="width: 18rem;">
                <img src="{{ asset('uploads/images/'~product.image)}}" class="card-img-top" alt="...">
                <div class="card-body">
                    <h5 class="card-title">{{ product.name }}</h5>
                    <p class="card-text">{{ product.price }} €</p>
                    <a href="{{ path('app_show_product',{'id':product.id}) }}" class="btn btn-outline-success ">Voir le Produit</a> 
                    <a href="{{ path('app_cart_new',{'id':product.id}) }}" class="btn btn-outline-success mt-2 ">Ajouter au panier</a>
                </div>
            </div>
        </div>
        
    {% endfor %}
    </div>

</div>
{% endblock %}
