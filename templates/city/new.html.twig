{% extends 'base.html.twig' %}

{% block title %}New City{% endblock %}

{% block body %}
    <div class="container">
        <h1>Create new City</h1>

        {{ include('city/_form.html.twig') }}<br>

        <a href="{{ path('app_city_index') }}" class="btn btn-primary">back to list</a>
    </div>
{% endblock %}
