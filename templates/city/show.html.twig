{% extends 'base.html.twig' %}

{% block title %}City{% endblock %}

{% block body %}
    <div class="container">
        <h1>City</h1>

        <table class="table">
            <tbody>
                <tr>
                    <th>Id</th>
                    <td>{{ city.id }}</td>
                </tr>
                <tr>
                    <th>Name</th>
                    <td>{{ city.name }}</td>
                </tr>
                <tr>
                    <th>ShippingCost</th>
                    <td>{{ city.shippingCost }}</td>
                </tr>
            </tbody>
        </table>

        <a href="{{ path('app_city_index') }}" class="btn btn-primary">Retour à la liste</a>

        <a href="{{ path('app_city_edit', {'id': city.id}) }}" class="btn btn-outline-info">Modifier</a>

        {{ include('city/_delete_form.html.twig') }}
    </div>
{% endblock %}
